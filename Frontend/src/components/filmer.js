import React, { Component } from 'react';
import { Grid, Cell} from 'react-mdl';

class Filmer extends Component {
    render() {
        return (
        <div style={{width: '100%', height:'100%', margin: 'auto'}}>
            <Grid className="home-grid"><h1>Filmer</h1>  </Grid>
        </div>
        )
    }
}

export default Filmer;